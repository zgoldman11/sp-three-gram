export declare class Queue<T extends any = undefined> {
    private initialCapacity;
    private container;
    private head;
    private tail;
    private length;
    private size;
    constructor(initialCapacity?: number);
    push(item: T): void;
    shift(expected?: T): T;
    isEmpty(): boolean;
    private double;
    private shrink;
}
